// Re-export references under a different name.
// Also a way to re-export the default reference from temperature as its own default! 
export { Thermostat as Thermo, default as default } from './108_temperature.mjs';
